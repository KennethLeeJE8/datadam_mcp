# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database User ID (UUID from Supabase auth.users table) (optional)
DATABASE_USER_ID=your-database-user-id

# Authentication Configuration (HTTP endpoints only)
# Server URL for OAuth metadata (your deployed Render URL)
SERVER_URL=https://datadam-mcp.onrender.com
# Enable/disable authentication for HTTP endpoints
REQUIRE_AUTH=true
# CORS allowed origins (comma-separated, or "*" for all)
ALLOWED_ORIGINS=*

# Optional: Additional configuration
NODE_ENV=development